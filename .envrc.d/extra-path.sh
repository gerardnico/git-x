# Add the dev script in the path (release, ...)
export PATH=$PROJECT_ROOT/contrib:$PROJECT_ROOT/contrib/release:$PATH

# When developing, we use the project library
export GITURE_BASH_LIB_PATH=$HOME/code/bash-lib/lib
